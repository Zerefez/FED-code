// Router configuration barrel export
// This file provides access to all routing configuration and navigation data
// Centralizes route definitions and navigation items for consistent use across the app

// Export route definitions and navigation configuration
// routes: Array of route objects for React Router configuration
// navigationItems: Array of navigation menu items with labels and paths
export { navigationItems, routes } from './routes';

// Export TypeScript types for navigation items
// Provides type safety when working with navigation configuration
export type { NavigationItem } from './routes';

