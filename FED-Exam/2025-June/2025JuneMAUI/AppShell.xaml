<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="_2025JuneMAUI.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:_2025JuneMAUI"
    xmlns:views="clr-namespace:_2025JuneMAUI.Views"
    Title="Eksamen System"
    BackgroundColor="{StaticResource White}">

    <TabBar>
        <ShellContent
            Title="Hjem"
            ContentTemplate="{DataTemplate local:MainPage}"
            Route="MainPage" />

        <ShellContent
            Title="Opret Eksamen"
            ContentTemplate="{DataTemplate views:ExamPage}"
            Route="ExamPage" />

        <ShellContent
            Title="Studerende"
            ContentTemplate="{DataTemplate views:StudentPage}"
            Route="StudentPage" />

        <ShellContent
            Title="Start Eksamen"
            ContentTemplate="{DataTemplate views:ExamSessionPage}"
            Route="ExamSessionPage" />

        <ShellContent
            Title="Historik"
            ContentTemplate="{DataTemplate views:HistoryPage}"
            Route="HistoryPage" />
    </TabBar>

</Shell>
